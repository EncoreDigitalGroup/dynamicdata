name: Push

on:
  push:
    branches: [ main ]

jobs:
  Dust:
    name: Duster
    uses: ./.github/workflows/duster-fix.yml
    permissions:
      contents: write
  Test:
    needs: Duster
    name: Run Tests
    uses: ./.github/workflows/pest.yml